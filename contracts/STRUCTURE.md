// PIL Protocol - Smart Contracts Structure

contracts/
  core/           # Core protocol contracts (state, proof, nullifier, disclosure)
  primitives/     # Privacy primitives (threshold crypto, VDF, TEE, confidential compute)
  bridge/         # Cross-chain bridge contracts (proof hub, atomic swap, relayer staking)
  compliance/     # Identity & compliance (KYC, AML, DID, credentials)
  infrastructure/ # Relayer network, governance, oracle, rate limiter, gas, recursive proofs
  interfaces/     # Shared interfaces and libraries
